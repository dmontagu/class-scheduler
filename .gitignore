# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Editor / OS
.DS_Store

# Local data (student data — keep out of git by default)
data/
*.csv
*.xlsx
!tests/**/*.csv
!tests/**/*.xlsx

# playwright scratch (screenshots, downloads, fixtures)
.playwright-mcp/
*.png

# editor / AI tooling (not part of the product)
.idea/
.claude/
.mcp.json

# committed anonymized example workbook (download from the app)
!web/public/*.xlsx
